-- Seeds removed: server-side seeding (initSeed.js) now creates users with properly hashed passwords
-- and initial bookings on server startup. Keep this file empty to avoid duplicate or plaintext passwords.